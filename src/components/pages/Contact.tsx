import { SectionTitle } from '../atoms';
import { ContactForm } from '../molecules';

export function Contact(): JSX.Element {
  return (
    <>
      <SectionTitle />
      <ContactForm />
    </>
  );
}
